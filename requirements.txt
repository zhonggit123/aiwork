# Word 解析
python-docx>=1.1.0

# 大模型 API（OpenAI 兼容，当前默认火山引擎豆包，也可接 通义/智谱/DeepSeek 等）
openai>=1.0.0
httpx>=0.25.0

# 浏览器自动化（可选，用于“模拟人手填表”）
playwright>=1.40.0

# 配置文件
pyyaml>=6.0

# 可选：接口直连提交
requests>=2.31.0

# 可视化 Web 界面
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 图片处理（Word 图片提取、TIFF 转 JPEG）
Pillow>=10.0.0

# JSON 修复（LLM 输出容错）
json-repair>=0.7.0

# 微软 Edge TTS（免费在线语音合成）
edge-tts>=6.1.0
